# Converted by an OPENAI API call using model: gpt-3.5-turbo-1106
from enum import Enum

class UsagePointConnectedKind(Enum):
    CONNECTED = 1
    PHYSICALLY_DISCONNECTED = 2
    LOGICALLY_DISCONNECTED = 3
